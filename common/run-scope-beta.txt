# Beta / 站点 A（`schedule_beta.yml`）全量 E2E scope；格式与 **`parse-run-scope.mjs`**、`run-scope-test.txt` 一致。
playground                  	1
signup                      	2
loginOut                    	3
account-settings            	4
blankModeling               	5
import-git                  	6
optimizationTemplateModeling	7
theoremTemplateMil          	8
modelingContestTemplate     	9
feedback                    	10
footer-links                	11
latex                       	12
project-list                	-1
# 规则：
#   - 以 # 开头的行为注释；空行忽略。
#   - 两列（Tab）：「脚本名（与 test/NN-<slug>.test.ts 的 <slug> 一致）」「执行顺序」。章节号由脚本名反查；**第二列为整数**，数值小的先跑；**`-1` 表示最后执行**（本文件示例为 **1～12** 与末行 **`-1`**）。
#   - 无 Tab：整行仅由章节号与空白组成（如「01 05」）。
#   - **#10**：反馈用例（GitHub 流程）；与历史上 **`#11`**（`11-feedback`）同样默认不纳入定时跑，需要时去掉 **`#`**。
#   - **project-list** 执行顺序通常为 **-1**（末位执行）。
